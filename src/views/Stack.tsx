import { Grid } from "@mui/material";
import ViewHeader from "../components/generics/ViewHeader";

const Stack = () => {
  return (
    <Grid container className="section">
      <ViewHeader title="Stack" />
    </Grid>
  );
};

export default Stack;
